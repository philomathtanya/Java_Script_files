var arr = [1, 2, 3, "tanya", "mathura"];
arr.push("varun dhawan");
for (let index = 0; index < arr.length; index ++) {
    console.log(arr[index]);

}
arr.pop();
for (let index = 0; index < arr.length; index++) {
    console.log(arr[index]);

}
arr.shift();
for (let index = 0; index < arr.length; index++) {
    console.log(arr[index]);

}
arr.unshift("ritesh verma");
for (let index = 0; index < arr.length; index++) {
    console.log(arr[index]);

}
